{
  "name": "knot",
  "version": "1.0.0",
  "description": "Knot of Love Fullstack Application",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:kill": "npx kill-port 5000 5173",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "seed:dev": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
